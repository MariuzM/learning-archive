import React from 'react';
import KeyImage from '../../assets/PageNotFound.svg';
import SVG from 'react-inlinesvg';

const PageNotFound = () => {
  return <SVG src={KeyImage} />;
};

export default PageNotFound;

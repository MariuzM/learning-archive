import { type SxProps } from '@mui/material';

export const s: SxProps = {
  textAlign: 'center',
  width: '100%',
};

export const fsfc: SxProps = {
  alignItems: 'flex-start',
  display: 'flex',
  flexDirection: 'column',
};
